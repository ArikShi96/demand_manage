1、M站代码打包.
npm install 
npm run build

2、友空间代码打包.
npm install
npm run build

3、后台代码打包
yarn install
yarn build

4、PC站代码，已发送@马力，@杜晨捷。

5、每个项目src下有一个api.js,如果需要配置多环境不同接口地址，可以在这里进行接口地址设置，
然后在package.json中scripts下进行命令配置。
